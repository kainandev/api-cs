namespace ApiCs.Models {
    // Enum = lista fixa de opções possíveis
    public enum TypeTicket {
        Normal = 0,  // preço cheio
        Middle   = 1,  // 50% de desconto
        VIP    = 2   // 50% mais caro
    }
}
